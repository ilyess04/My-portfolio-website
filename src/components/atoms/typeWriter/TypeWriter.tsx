import Typewriter from "typewriter-effect";
import { INIT_TYPE_WRITER_OPTIONS } from "../../../common/consts";

const Type = (): JSX.Element => {
  return <Typewriter options={INIT_TYPE_WRITER_OPTIONS} />;
};

export default Type;
